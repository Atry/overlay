boolean:
  Boolean:
    if:
      then: {}
      else: {}
      return: {}
  "True":
    - [Boolean]
    - if:
        then: {}
        else: {}
        return: [then]
  "False":
    - [Boolean]
    - if:
        then: {}
        else: {}
        return: [else]
  not:
    - operand:
        - [Boolean]
      _if:
        - [operand, if]
        - then:
            - ["False"]
          else:
            - ["True"]
      return:
        - [_if, return]
  and:
    - operand0:
        - [Boolean]
      operand1:
        - [Boolean]
      _if:
        - [operand0, if]
        - then:
            - [operand1]
          else:
            - ["False"]
      return:
        - [_if, return]
  or:
    - operand0:
        - [Boolean]
      operand1:
        - [Boolean]
      _if:
        - [operand0, if]
        - then:
            - ["True"]
          else:
            - [operand1]
      return:
        - [_if, return]

nat:
  - [boolean]
  - Nat:
      - is_zero:
          return: [nat, ~, Boolean]
  - Zero:
      - [Nat]
      - is_zero:
          return: [nat, ~, "True"]
  - Succ:
      - [Nat]
      - predecessor:
          - [Nat]
      - is_zero:
          return: [nat, ~, "False"]
